package ui.settings;

import javax.swing.*;
import java.awt.*;

public class SettingsPanel extends JPanel {
  // Settings module home screen

    public SettingsPanel() {

        setLayout(new BorderLayout());

        add(new JLabel("SETTINGS MODULE"), BorderLayout.CENTER);
    }
}
